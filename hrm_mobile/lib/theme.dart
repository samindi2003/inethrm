import 'package:flutter/material.dart';

// Ocean Blue Theme Palette

// Backgrounds
const Color bgDark = Color(0xFF0A192F); // Deep Navy
const Color bgCard = Color(0xFF112240); // Card Navy

// Accents
const Color primaryColor = Color(0xFF64FFDA); // Bright Aqua
const Color accentColor = Color(0xFF00B4D8); // Cyan

// Typography
const Color textMain = Color(0xFFE6F1FF); // Crisp White
const Color textMuted = Color(0xFF8892B0); // Blue-Grey

// Utilities
const Color borderColor = Color(0xFF233554); // Subtle Border Navy
const Color dangerColor = Color(0xFFFF6B6B); // Soft Red for errors
const Color successColor = Color(0xFF00C896); // Ocean Green
const Color warningColor = Color(0xFFFFB74D); // Warm Sand
const Color primaryHover = Color(0xFF33D9B2); // Lighter Aqua for hover
const Color bgDarker = Color(0xFF050D1A); // Deeper Navy for contrasts
